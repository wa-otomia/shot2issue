{
  "name": "@shot2issue/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared core for shot2issue: canvas annotation engine, issue providers, AI/OAuth helpers, storage and i18n. Framework-agnostic and platform-free; consumed as source by apps/extension and apps/desktop via platform adapters.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./canvas": "./src/canvas/engine.ts",
    "./providers": "./src/providers/index.ts",
    "./ai": "./src/ai/ai.ts",
    "./ports": "./src/ports.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "node tests/run.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
